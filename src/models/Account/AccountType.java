package models.Account;

/**
 * The {@code AccountType} enum represents the types of user accounts,
 * distinguishing between a Bank Account and a Wallet Account.
 **/
public enum AccountType {
    /**
     * Represents a Bank Account type.
     */
    BankAccount,
    /**
     * Represents a Wallet Account type.
     */
    WalletAccount;
}
